---
services:
  ocis:
    configs:
      - source: apps_yaml
        target: /etc/ocis/apps.yaml
    depends_on:
      ai-folder-brief-sidebar-init:
        condition: service_completed_successfully

  ai-folder-brief-sidebar-init:
    image: owncloud/web-extensions:ai-folder-brief-sidebar-0.1.0
    user: root
    volumes:
      - ocis-apps:/apps
    entrypoint:
      - /bin/sh
    command: ["-c", "cp -R /var/lib/nginx/html/ai-folder-brief-sidebar/ /apps"]
