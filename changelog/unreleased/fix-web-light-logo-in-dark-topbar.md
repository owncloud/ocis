fix(web): Use light logo in light mode (with dark blue topbar)

The light theme actually have a dark blue topbar and login screen, where the dark logo is applied and can hardly be seen. This patch changes it to logo.svg (the light logo).

https://github.com/owncloud/ocis/pull/12782
