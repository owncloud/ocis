apiVersion: v1
kind: Service
metadata:
  name: mailpit
spec:
  selector:
    app: mailpit
  ports:
    - name: smtp
      port: 1025
      targetPort: smtp
      protocol: TCP
      appProtocol: tcp
    - name: web
      port: 8025
      targetPort: web
      protocol: TCP
