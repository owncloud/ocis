services:
  acceptance-tests:
    image: owncloudci/php:7.4
    command: /bin/bash /tests/run-tests.sh
    environment:
      OCIS_ROOT: /drone/src
      TEST_OCIS: "true"
      RUN_ON_OCIS: "true"
      TEST_SERVER_URL: https://ocis-server:9200
      TEST_WITH_GRAPH_API: "true"

      STORAGE_DRIVER: $STORAGE_DRIVER
      TEST_SOURCE: $TEST_SOURCE
      BEHAT_SUITE: ${BEHAT_SUITE:-}
      BEHAT_FEATURE: ${BEHAT_FEATURE:-}
      DIVIDE_INTO_NUM_PARTS: $DIVIDE_INTO_NUM_PARTS
      RUN_PART: $RUN_PART
    env_file:
      - ../../../../.drone.env
    volumes:
      - ./run-tests.sh:/tests/run-tests.sh
      - oCISownCloud10testsuite:/drone/src
      - ../../../../:/drone/src
      - ../../../../vendor-bin/behat/composer.json:/tmp/vendor-bin/behat/composer.json
      - oCISownCloud10testsuite-vendor:/drone/src/vendor

volumes:
  oCISownCloud10testsuite:
  oCISownCloud10testsuite-vendor:
