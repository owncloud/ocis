---
pkg: "assets"
dest: "."
output: "embed.go"
fmt: true
noprefix: true

compression:
  compress: true

custom:
  - files:
    - "../../phoenix/"
    base: "../../phoenix/"
    prefix: ""
    exclude:
    - "appinfo/**"
    - "LICENSE/**"
    - "CHANGELOG.md"
    - "README.md"
    - "config.json"

...
